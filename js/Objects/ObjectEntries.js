//* Object.entries(): Este método devuelve un array de pares clave-valor para las propiedades enumerables de un objeto dado

const obj = { a: 1, b: 2, c: 3 };
const entries = Object.entries(obj);
console.log(entries);
